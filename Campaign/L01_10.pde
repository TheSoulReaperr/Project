void drawLevel1() {
  drawMountains();
  drawClouds();
  drawCommon();
}

void drawLevel2() {
  drawHills();
  drawClouds();
  drawCommon();
}

void drawLevel3() {
  drawPlatforms();
  drawClouds();
  drawCommon();
}

void drawLevel4() {
  drawStars();
  drawMountains();
  drawClouds();
  drawCommon();
}

void drawLevel5() {
  drawStars();
  drawPlatforms();
  drawClouds();
  drawCommon();
}

//---------------------------------------------------------------------------------------------------------------------------------------------------------------------------

void drawLevel6() {
  drawShips(520);
  drawSeas(650);
  drawClouds();
  drawCommon();
}

void drawLevel7() {
  drawShips(500);
  drawSeas(630);
  drawClouds();
  drawCommon();
}

void drawLevel8() {
  drawShips(490);
  drawSeas(610);
  drawClouds();
  drawCommon();
}

void drawLevel9() {
  drawShips(470);
  drawStars();
  drawSeas(590);
  drawClouds();
  drawCommon();
}

void drawLevel10() {
  drawShips(450);
  drawStars();
  drawSeas(570);
  drawClouds();
  drawCommon();
}
