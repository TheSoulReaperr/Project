void drawLevel1() {
  drawMountains();
  drawClouds();
  drawCommon();
}

void drawLevel2() {
  drawHills();
  drawClouds();
  drawCommon();
}

void drawLevel3() {
  drawPlatforms();
  drawClouds();
  drawCommon();
}

void drawLevel4() {
  drawStars();
  drawMountains();
  drawClouds();
  drawCommon();
}

void drawLevel5() {
  drawStars();
  drawPlatforms();
  drawClouds();
  drawCommon();
}

//---------------------------------------------------------------------------------------------------------------------------------------------------------------------------

void drawLevel6() {
  drawShips(670*m);
  drawSeas(650*m);
  drawClouds();
  drawCommon();
}

void drawLevel7() {
  drawShips(650*m);
  drawSeas(630*m);
  drawClouds();
  drawCommon();
}

void drawLevel8() {
  drawShips(630*m);
  drawSeas(610*m);
  drawClouds();
  drawCommon();
}

void drawLevel9() {
  drawShips(610*m);
  drawStars();
  drawSeas(590*m);
  drawClouds();
  drawCommon();
}

void drawLevel10() {
  drawShips(580*m);
  drawStars();
  drawSeas(570*m);
  drawClouds();
  drawCommon();
}
